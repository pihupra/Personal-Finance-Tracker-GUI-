# Personal Finance Tracker (GUI)

A desktop GUI app to track income, expenses, savings goals and show monthly summaries and charts.
Built with Python, Tkinter and matplotlib.

## Run locally / in Codespaces

1. Install dependencies:
```bash
pip install -r requirements.txt
